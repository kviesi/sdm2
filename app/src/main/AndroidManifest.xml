<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifspsaocarlos.sdm.jogostabuleiro" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/tabuleiro"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activity.SortActivity" />
        <activity android:name=".activity.DiceActivity" />
        <activity android:name=".activity.JokenpoActivity" />
        <activity android:name=".activity.CountDownActivity" />
        <activity android:name=".activity.MenuActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.HistoricActivity" >
        </activity>
    </application>

</manifest>
